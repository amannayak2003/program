package LinkedList;

public class linkedlist {
       public static class node {
        int data;
        node next;

        public node(int d){
            this.data = d;
            this.next = null;
        }
       }
}
